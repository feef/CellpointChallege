query GetOrganizationRepositories($name: String!) {
  organization(login: $name) {
    name
    repositories(first: 100) {
      nodes {
        name
        stargazers {
          totalCount
        }
        primaryLanguage {
          name
        }
        owner {
          login
        }
      }
    }
  }
}
